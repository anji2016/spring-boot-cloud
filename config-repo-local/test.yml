server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/D/workspace/spring-boot-cloud/config-repo-local

logging:
  level:
    org.springframework: INFO
